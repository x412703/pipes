apiVersion: v1
kind: Service
metadata:
  name: service1
  labels:
    app.kubernetes.io/name: service1
spec:
  selector:
    app.kubernetes.io/name: service1
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
